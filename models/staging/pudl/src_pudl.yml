version: 2

sources:
  - name: pudl
    database: s_dev_pudl
    schema: bronze
    tables:
      - name: state_fips_map
      - name: stable_out_ferc714__hourly_estimated_state_demand_bronze
        loaded_at_field: ingest_date